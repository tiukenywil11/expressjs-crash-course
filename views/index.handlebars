<h1 class="text-center mb-3">
    {{title}}
</h1>

<h4> Members </h4>

<!-- 'members' comes from index.js -->
<ul class="list-group"> 
    {{#each members}}
    <li class="list-group-item"> {{this.name}}: {{this.email}} </li>
    {{/each}}
</ul>